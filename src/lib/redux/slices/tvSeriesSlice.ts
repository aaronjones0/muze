import { createSlice } from "@reduxjs/toolkit";

export const tvSeriesSlice = createSlice({
  name: 'tv-series',
  initialState: {},
  reducers: {
    add: state => {
      // Post
    },
    modify: state => {
      // Patch
    }
  }
})